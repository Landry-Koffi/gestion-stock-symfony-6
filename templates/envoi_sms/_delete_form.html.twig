<form method="post" action="{{ path('app_envoi_sms_delete', {'id': envoi_sm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ envoi_sm.id) }}">
    <button class="btn">Delete</button>
</form>
