{% extends 'base.html.twig' %}

{% block title %}CommandeClient{% endblock %}

{% block body %}
<section class="section">
    <div class="container-fluid">
    <h1>CommandeClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_client.id }}</td>
            </tr>
            <tr>
                <th>NumeroCommande</th>
                <td>{{ commande_client.numeroCommande }}</td>
            </tr>
            <tr>
                <th>DateCommandeAt</th>
                <td>{{ commande_client.dateCommandeAt ? commande_client.dateCommandeAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ commande_client.observation }}</td>
            </tr>
            <tr>
                <th>TotalHt</th>
                <td>{{ commande_client.totalHt }}</td>
            </tr>
            <tr>
                <th>TotalTva</th>
                <td>{{ commande_client.totalTva }}</td>
            </tr>
            <tr>
                <th>TotalTtc</th>
                <td>{{ commande_client.totalTtc }}</td>
            </tr>
            <tr>
                <th>EtatTraite</th>
                <td>{{ commande_client.etatTraite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>EtatValide</th>
                <td>{{ commande_client.etatValide ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ commande_client.updatedAt ? commande_client.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ commande_client.deletedAt ? commande_client.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>NumeroFacture</th>
                <td>{{ commande_client.numeroFacture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_client_index') }}">back to list</a>

    <a href="{{ path('app_commande_client_edit', {'id': commande_client.id}) }}">edit</a>

    {{ include('commande_client/_delete_form.html.twig') }}
    </div>
</section>
{% endblock %}
