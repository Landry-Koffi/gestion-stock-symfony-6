{% extends 'base.html.twig' %}

{% block title %}CommandeFournisseur{% endblock %}

{% block body %}
    <h1>CommandeFournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_fournisseur.id }}</td>
            </tr>
            <tr>
                <th>NumeroCommande</th>
                <td>{{ commande_fournisseur.numeroCommande }}</td>
            </tr>
            <tr>
                <th>DateCommandeAt</th>
                <td>{{ commande_fournisseur.dateCommandeAt ? commande_fournisseur.dateCommandeAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ commande_fournisseur.observation }}</td>
            </tr>
            <tr>
                <th>EtatTraite</th>
                <td>{{ commande_fournisseur.etatTraite ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>EtatValide</th>
                <td>{{ commande_fournisseur.etatValide ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ commande_fournisseur.updatedAt ? commande_fournisseur.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ commande_fournisseur.deletedAt ? commande_fournisseur.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>NumeroFacture</th>
                <td>{{ commande_fournisseur.numeroFacture }}</td>
            </tr>
            <tr>
                <th>DateLivraisonAt</th>
                <td>{{ commande_fournisseur.dateLivraisonAt ? commande_fournisseur.dateLivraisonAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_fournisseur_index') }}">back to list</a>

    <a href="{{ path('app_commande_fournisseur_edit', {'id': commande_fournisseur.id}) }}">edit</a>

    {{ include('commande_fournisseur/_delete_form.html.twig') }}
{% endblock %}
