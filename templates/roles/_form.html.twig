{{ form_start(form) }}
    {{ form_row(form.libelle, {
        attr: {
            class: "form-control"
        }
    }) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
